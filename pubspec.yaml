name: cliproxy_dash
description: A privacy-conscious Android quota dashboard for multiple AI model providers.
publish_to: 'none'
version: 2.0.9+14

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_secure_storage: ^10.3.1
  http: ^1.6.0
  intl: ^0.20.2
  path_provider_android: 2.3.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
